{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist node_modules/.cache",
    "build:clean": "npm run clean && npm run build"
  },
  "devDependencies": {
    "vite": "^3.0.7"
  }
}